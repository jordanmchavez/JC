#include "JC/Random.h"

namespace JC::Random {

//--------------------------------------------------------------------------------------------------

static u64 Next(u64 x, u32 k) {
	return (x << k) | (x >> (64 - k));
}

static u64 Next(u64 s[2]) {
	
}

//--------------------------------------------------------------------------------------------------

}	// namespace JC::Random